import React from 'react';

// This file is kept simple or empty as I moved the controls to App.tsx for better layout context.
// However, to satisfy the structure if needed later, I'll export a dummy.
export const Controls = () => {
  return null;
};